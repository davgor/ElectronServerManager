# 010.1 — Introduce structured logger

Add `electron-log` or a thin `src/main/logger.ts` wrapper with levels. Replace `console.log`/`console.error` in main and steamDetection.

**Origin:** Code review — logging sprawl.

## Acceptance criteria

- [ ] `src/main/logger.ts` exports `debug`, `info`, `warn`, `error`
- [ ] Main process files use logger instead of raw console (except tests)
- [ ] Logger writes to file in production (`electron-log` default behavior)
- [ ] `npm test`, `npm run lint` pass
