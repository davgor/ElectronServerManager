# 004.3 — Persist server settings via `electron-store`

Wire `electron-store` (already a dependency) to persist auto-restart, auto-update, backup locations, and backup intervals. Replace ad-hoc `localStorage` for backups.

**Origin:** Code review — settings lost on restart; unused dependency.

## Acceptance criteria

- [ ] Main process exposes typed IPC to read/write settings (or preload wraps store in main)
- [ ] Auto-restart, auto-update, backup path, and interval persist across sessions
- [ ] `localStorage` usage for backups removed
- [ ] Tests cover load/save round-trip
- [ ] `npm test`, `npm run lint`, `npm run electron-build` pass
