# 003.3 — Expose typed API methods on preload context bridge

Replace generic `ipcRenderer.invoke` exposure with named methods (`getSteamServers`, `runServer`, etc.) and typed `windowControls`.

**Origin:** Code review — depends on 003.2.

## Acceptance criteria

- [ ] `preload.ts` exposes typed methods; generic `invoke` is not exposed to renderer (or is internal only)
- [ ] `electron.d.ts` reflects the new typed `window.electron` API
- [ ] Preload tests verify each method invokes the correct channel
- [ ] `npm test`, `npm run type-check` pass
