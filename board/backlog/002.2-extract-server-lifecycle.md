# 002.2 — Extract server lifecycle IPC into `src/main/serverProcess.ts`

Consolidate duplicated kill-command construction and spawn logic from `run-server`, `stop-server`, and `auto-update-server` into shared helpers.

**Origin:** Code review — duplicated platform-specific kill/spawn in three handlers.

## Acceptance criteria

- [ ] `src/main/serverProcess.ts` exports `startServer`, `stopServer`, and shared kill helper
- [ ] `main.ts` IPC handlers delegate to `serverProcess.ts`
- [ ] Kill/spawn logic is defined once (no copy-paste between run/stop/auto-update)
- [ ] Unit tests mock `child_process` and cover win32 vs unix kill commands
- [ ] `npm test`, `npm run lint`, `npm run type-check` pass
