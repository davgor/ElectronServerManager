# 002.5 — Slim `main.ts` to bootstrap + handler registration

After 002.1–002.4, refactor `main.ts` to only handle app lifecycle, window creation, and IPC registration.

**Origin:** Code review — depends on 002.1–002.4.

## Acceptance criteria

- [ ] `main.ts` is under ~200 lines
- [ ] No business logic remains inline (only wiring)
- [ ] `npm run electron-build` and full test suite pass
- [ ] Manual smoke: app launches, servers list loads, run/stop still work
