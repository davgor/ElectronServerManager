# 012.5 — Document release + auto-update runbook

Document how releases produce updater metadata, which platforms auto-update, and how to verify an update end-to-end (including using the in-app version string).

**Depends on:** 012.2–012.4 ideally done so docs match shipped behavior.

## Acceptance criteria

- [ ] README (or linked doc) explains: push/release → GitHub Release assets → app checks → user restarts
- [ ] Notes which installers support auto-update vs manual reinstall
- [ ] Includes a short verification checklist (install old build, publish newer release, confirm version bump in UI)
- [ ] Architecture/docs mention `electron-updater` and where version is displayed (coordinate with 008 if that epic is still open)
