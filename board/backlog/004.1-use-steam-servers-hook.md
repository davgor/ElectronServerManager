# 004.1 — Extract `useSteamServers` hook with stable polling

Move server fetching, path selection, and 10s/30s polling into a dedicated hook with stable callbacks (functional state updates, refs where needed).

**Origin:** Code review — App.tsx complexity.

## Acceptance criteria

- [ ] `src/renderer/hooks/useSteamServers.ts` owns fetch + poll logic
- [ ] `fetchServers` does not close over stale `servers` array for change detection
- [ ] Hook exposes `servers`, `loading`, `error`, `refresh`, `selectedPath`, `setSelectedPath`
- [ ] Unit tests cover polling and error retry behavior
- [ ] `npm test`, `npm run lint` pass
