# 004.2 — Extract `ServerCard` component

Split per-server UI (status, run/stop, settings, backup controls) from `App.tsx` into `src/renderer/ServerCard.tsx`.

**Origin:** Code review — App.tsx complexity.

## Acceptance criteria

- [ ] `ServerCard.tsx` renders a single server with all actions/settings
- [ ] Props are typed; no prop-types needed (TypeScript)
- [ ] `App.test.tsx` still passes; add focused `ServerCard.test.tsx`
- [ ] `npm test`, `npm run lint`, `npm run build` pass
