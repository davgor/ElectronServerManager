# 003.2 — Define shared IPC types in `src/types/ipc.ts`

Create typed request/response interfaces for every IPC channel (`GetSteamServers`, `RunServer`, `BackupServerSave`, etc.).

**Origin:** Code review — typed IPC bridge epic.

## Acceptance criteria

- [ ] `src/types/ipc.ts` exports typed shapes for all 15+ IPC channels
- [ ] Types are importable from main, preload, and renderer without circular deps
- [ ] `src/__tests__/types/ipc.test.ts` validates type exports (compile-time + smoke)
- [ ] `npm run type-check` passes
